# Base URL of the Spring Boot backend. Copy this file to .env and adjust if needed.
VITE_API_BASE_URL=http://localhost:8080

# Demo user id — the backend has no auth yet (see backend README §10),
# so the frontend passes this as a required query param on every call.
VITE_DEMO_USER_ID=1
